*_SoftMaxWithLoss 파일들은 ex3 의 oneVsAll 에서 class 의 가짓수 만큼 반복하지 않고 단 한번의 loop 로 학습하는 것을 구현한 code.

sigmoid with loss 대신에 softmax with loss(cross entropy error) 를 이용했으며

Deep Learning from Scratch 에서 배웠던 내용을 기반으로 작성하였다.
