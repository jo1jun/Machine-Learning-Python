# PCA는 overfitting 을 줄이기 위해 사용되지는 않지만 PCA자체가 overfitting 될 수도 있지 않을까? (training set 를 제외한 데이터들이 분산 차이를 꽤 높일 수 있는 경우)
# 강의에서는 따로 언급이 없었다. 따라서 걱정할 필요가 없나보다. 그저 차원 감소 기법으로 이해하자.
# CV set 은 PCA 적용 이후 다른 hyperparameter 들 검증하기 위해서 사용한다.
